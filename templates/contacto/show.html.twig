{% extends 'base.html.twig' %}

{% block title %}Contacto{% endblock %}

{% block body %}
    <h1>Contacto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contacto.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ contacto.nombre }}</td>
            </tr>
            <tr>
                <th>Correo</th>
                <td>{{ contacto.correo }}</td>
            </tr>
            <tr>
                <th>Mensaje</th>
                <td>{{ contacto.Mensaje }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contacto_index') }}">back to list</a>

    <a href="{{ path('app_contacto_edit', {'id': contacto.id}) }}">edit</a>

    {{ include('contacto/_delete_form.html.twig') }}
{% endblock %}
